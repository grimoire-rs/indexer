# mirror

A second namespace on a second forge — nested (`gitlab.com/team`), a GitLab
`login` owner rather than a GitHub one, and a registry host that is not
`ghcr.io`. Namespace grouping and the filter chips are what this is for.
