# rust-style

Carries a rating thread that exists and has **no votes** — the card shows a
`0`, which is a different thing from an unrated card showing nothing.

One support channel only, so the help section renders with a single row.
