# bean-counter

The only ref whose `.stats.json` entry carries a `downloads` blob and no
`rating`. Nothing here renders it — `downloads` is an *unknown* key, not a
specification — and the card is simply unrated. What it proves is that the
build republishes a stat key it does not understand, untouched.
