# old-helper

Deprecated **with** a reason and a replacement, so the banner carries both:
`deprecated` is the publisher's own message — grim shows the same string —
and `replacedBy` is a separate field that rides after it.

Hidden from the default browse; it appears only once the deprecated toggle
is on, and that toggle now survives a Back from this page.
