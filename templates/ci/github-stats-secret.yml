      #
      # Set GRIM_DOWNLOADS_TOKEN as a repository secret, to an Artifactory
      # access token with read on those repositories. To exchange a
      # short-lived token through OIDC instead - no stored secret at all - set
      # `downloads.oidcProvider` in index.config.json and re-render.
